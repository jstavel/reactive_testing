---
topic: Presentation report: Gherkin↔corpus links, report.json sibling, sample fixture, CI wiring
goal: Raise the project to industry-standard engineering: deterministic machine-readable reports, a browsable committed evidence fixture, an offline regression gate, and a fully green CI/CD surface on GitHub Pages
updated: 2026-09-11T14:28
---

- (direction) Source: tasks.org presentation-task (headhunter/AI-optimization) + live review of reporter/ and orchestrator/ code. Express mode, single slug spec.
- (event) As-is finding: summary bar, accordion Gherkin tree, run-time Gherkin snapshot, per-step screenshots, model linkage and report:smoke CLI already exist (reporter/html-report.ts, bin/report-smoke.ts, handlinks @last-run/@last-fail). The "Gherkin→corpus report missing" claim in tasks.org is stale. Delta = evidence links, JSON sibling, fixture+generator, CI facade.
- (decision) Committed example corpus comes only from a MOCK fixture, never a real recording — live snapshots carry real Kraken account balances (e.g. "Balance 5 035.24 USD") = personal financial data. corpus/ stays gitignored; fixture lives at corpus/example/ un-ignored via "corpus/*" + "!corpus/example".
- (decision) Fixture is all-pass, trimmed (a few step snapshots + all probes + present screenshots, ~1-2 MB) and committed so headhunter assistants can browse corpus + report directly in the repo; CI validates the same fixture (regression gate for the offline validator+reporter pipeline, no browser).
- (constraint) No failing evidence is ever committed to corpus/example/: CI badge stays 100% green — a red badge would poison the job-search signal. Failure showcases are generated on demand / in README only, never CI-guarded.
- (capability) CAP-1 (report.json): the report gains a deterministic machine-readable sibling report.json next to report.html summarizing the same run (scenario index with per-step evidence paths + CI-facing top counts), byte-stable under NFR-1 determinism.
- (decision) report.json serves both audiences at once: AI assistants read it without rendering HTML; CI/CD consumes it to fail the job, emit a step summary, and drive the README "Tests" badge. The CI "reads JSON" pattern also retires the roadmap "xunit/CI output reporters" item.
- (constraint) report.json carries a versioned schema tag at the top ("schema": "report.v1"); bump when the shape changes since CI consumes it. Arrays are deterministically ordered for byte-stability.
- (decision) report.json is a scenario-facing index, distinct from run-manifest.json (storage inventory). It is produced by the same pure builder as report.html (reporter module, NFR-1), not by walking the manifest at report time.
- (decision) Per-step corpus evidence in the report is extended from screenshot-only to snapshot (pre/post), probe, and network refs. StepEvidence (model/schemas.ts) gains the explicit ref fields (like screenshot today) instead of relying on manifest name-order inference; regenerated plan + tests follow.
- (decision) CI page is fully green: typecheck, unit tests, validate:smoke over the committed fixture, then a determinism gate — regenerate the sample report into a temp dir from the mock generator and git diff --exit-code against the committed one (NFR-1 byte-identity proves fixture ↔ generator lockstep).
- (constraint) validate:smoke and report:smoke CLIs gain a --corpus-dir flag (today they default to local corpus/ and the @last-run fan) so CI and the sample flow can target corpus/example explicitly without ever touching local recorded runs.
- (decision) Story split (4 stories confirmed): D1 reporter work (report.json sibling of report.json + per-step snapshot/probe/network links + StepEvidence extension); D2 sample-report generator + committed mock fixture corpus/example/ + --corpus-dir + README error screenshot; D3 failure-demo story (throwaway red report + README screenshot, zero committed corpus, not CI-guarded); D4 CI workflow importing D1+D2 (validate + determinism gate + badges + Pages).
- (assumption) The committed fixture is print-ready: screenshots included so the report img srcres work on GitHub Pages; the fixture contains no @last-run/@last-fail symlinks (symlinks are not first-class in git/Pages), those fans remain a local-run convenience.
- (event) Self-validate Pass 1 (Coherence): all 6 capabilities have intent+success (WHAT-level, testable; constraints bend design (mock-only+no-failining rule out committing real or red evidence; versioned schema and --corpus-dir rule out unversioned/untargeted flows); 6 explicit non-goals proper; success signal concrete and demo-visible; CAP IDs unique in folder; prose lean.
- (event) Self-validate Pass 2 (Preservation): load-bearing claims landed — stale-as-is finding in Why; mock-only+privacy in CAP-3+constraints+non-goals; green-badge rule (constraint+non-goal); report.json dual-audience (CAP-1+Why; versioned schema, scenario-vs-manifest, --corpus-dir, offline-only, no-symlink-fixture (,constraints+assumption); determinism gate)CAP-4+CAP-5; StepEvidence extension in CAP-2. Story split deferred to Story Breakdown (stories.yaml), not kernel content. Wrapper-only dropped: codebase digging notes, file sizes, conversation process.
- (decision) Story 1 (report.json sibling + per-step snapshot/probe/network evidence links, StepEvidence ref extension). spec_checkpoint=true, done_checkpoint=true, Jan OK. Dispatch: emit from the same pure builders as report.html, byte-stable sorted arrays + "schema":"report.v1" tag, report:smoke emits both.
- (decision) Story 2 (deterministic sample-report generator → committed all-pass mock fixture corpus/example/ -browsable; --corpus-dir on validate/report CLIs). spec_checkpoint=true, done_checkpoint=true, Jan OK. Dispatch: fixture layout mirrors a real run exactly, un-ignore via corpus/* + !corpus/example, mock-only/all-pass/trimmed, no @last-run/@last-fail symlinks in fixture, commit generated output + report.
- (decision) Story 3 (failure-demo: on-demand red sample report + README screenshot of FAIL view, zero committed corpus, not CI-guarded). spec_checkpoint=false, done_checkpoint=false. Dispatch: generate-sample-report --fail writes a throwaway failing variant outside corpus/example, README screenshot approved visually, never alter the fixture or any CI gate.
- (decision) Story 4 (CI/CD: green gate over committed fixture, determinism diff gate, badges, GitHub Pages publish). spec_checkpoint=false, done_checkpoint=true. Dispatch: typecheck+tests+validate:smoke --corpus-dir corpus/example+regen temp-dir git diff --exit-code determinism gate, CI status + Tests (from report.json) badges, Pages publish from artifact, job stays green-only.
- (event) stories.yaml written and validated against stories-schema: 4 entries, ids "1"-"4" quoted strings, unique, prefix-free, all required fields present, no status field; descriptions two sentences pointing into SPEC capabilities; checkpoints captured from Jan
- (decision) S2 fixture scope: the committed corpus/example fixture omits screenshots and network files in v1 — network is never read offline and screenshots are existence-filtered, so the report renders evidence links only (MISSING_KIND exercised naturally). Supersedes assumption #1 (representative screenshots).
- (decision) Framing correction (Jan, 2026-09-11): the outreach/headhunter angle is a side effect, not the goal. All spec text is re-anchored to industry-standards compliance: reproducible artifacts, machine-readable reports, browsable evidence, offline regression gates, green CI/CD. Headhunter phrasing removed from the specs (SPEC.md Why/Success, S2/S3/S4 story specs); memlog history is append-only and keeps the earlier wording as record.
- (decision) Story 5 (README failure-shots showcase — the S3 split followup): dev-only Playwright rasterizer bin/screenshot-report.ts + screenshot:report script rendering a fail-demo report to committed docs/report-failure.png; README "Error report showcase" block (image + regenerate commands + not-committed/not-CI-guarded note, industry-standards framing); docs/usage.md entry. spec_checkpoint=true, done_checkpoint=true.
