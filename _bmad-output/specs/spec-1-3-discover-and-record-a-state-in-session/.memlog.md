---
topic: Story 1.3 — Discover-and-record a state in-session
updated: 2026-08-25T23:09
---

- (direction) Source: epics.md Story 1.3 definition. AI reads live DOM/aria to discover new states/contracts; QE adjudicates; entries land in fsm.ts/contracts.ts.
- (decision) Capability split: CAP-1 = AI observes live browser and proposes; CAP-2 = QE adjudicates before model update; CAP-3 = accepted entries committed to model files immediately.
- (constraint) AI reads browser for discovery (what to model), NOT for specifying implementation details like xpaths/selectors — that is Epic 2's Orchestrator.
- (constraint) Classification per state-granularity.md: URL change → state, action → contract, data value → parameter, tooltip/hover → ignore.
- (constraint) Read-only flows only (NFR-3). No Playwright or test runtime in model files; abstract action/guard signatures only (Epic 2 owns runtime).
- (constraint) Nothing enters fsm.ts/contracts.ts without QE adjudication (FR-2, AD-10). English-only identifiers (NFR-4).
- (assumption) QE has an active browser session with Kraken Pro loaded. The AI has access to the page source (DOM/aria tree).
- (assumption) The existing model (fsm.ts/contracts.ts from Story 1.2) is the baseline. Discovery adds to it, does not replace it.
- (question) Should the AI propose all observations (including ones already modeled) and let dedup be the QE's job, or should the AI filter out known states before proposing?
- (event) Self-validate pass 1 (coherence): All 3 capabilities have intent+success. Constraints bend design decisions. Non-goals explicit. Success signal testable. IDs unique. Spec Law rules 1-6,8 satisfied.
- (event) Self-validate pass 2 (preservation): All load-bearing claims from epics.md Story 1.3 landed in kernel or companions. FR-1 (record state), FR-2 (QE owns), AD-10 (no silent edits), state-granularity.md (classification rules) all preserved.
- (decision) AI pre-filters proposals to exclude already-modeled states/contracts before presenting to QE. Dedup logic lives in the discovery step, not as a separate story concern.
- (decision) DOM/aria access via MCP Playwright connecting to localhost:9222 with Kraken Pro already open in the browser. No browser extension or custom tooling needed.
- (event) Open questions resolved: (1) AI pre-filters already-modeled items, (2) MCP Playwright at localhost:9222 with Kraken Pro open. SPEC.md updated accordingly.
- (event) Live review session completed. Home Page reviewed against DOM/aria via MCP Playwright at localhost:9222. 2 state proposals (marketSelectorDialog, userSettingsDialog) and 5 contract proposals (clickWithdraw, clickTransfer, selectBalancesTab, selectOrdersTab, selectPositionsTab) all deferred as out of current scope. Model is complete for read-only critical path. No changes to fsm.ts or contracts.ts.
