---
topic: Repo hygiene: Biome linting + CONTRIBUTING.md
goal: Consistent static-analysis/style gate (Biome) wired into scripts and CI, plus a CONTRIBUTING.md that documents the BMad workflow against the current repo setup
updated: 2026-09-11T15:51
---

- (direction) Source: tasks.org optional items (linting Biome + CONTRIBUTING.md) + recon. One hygiene story. CONTRIBUTING.md does not exist (create, not update); Biome not installed yet; no lint in CI; repo style single-quote/2-space/~100-col.
- (decision) Biome config mirrors the repo style to keep format churn minimal: quoteStyle single, semicolons always, indent space 2, lineWidth ~100; lint recommended with mechanical-safe fixes applied (--write/--unsafe) and any noisy rule disabled with rationale; formatter enabled (repo-matching). npm scripts lint / lint:fix; CI gains a lint step in ci.yml (green-only). CONTRIBUTING.md created to document the BMad workflow + current commands/layout/branch policy.
- (capability) CAP-1 (lint gate): repo has a configurable static-analysis/style gate (Biome) with scripts and a CI step, green on the current tree with minimal style churn.
- (capability) CAP-2 (contribution doc): CONTRIBUTING.md exists and matches the BMad workflow + current repo setup (commands, layout, model-SSOT, branch/PR policy).
- (event) Self-validate Pass 1 (Coherence): CAP-1/CAP-2 each have intent+success; constraints bend design (style-matching config, green-only CI step, create-not-duplicate CONTRIBUTING); 4 non-goals explicit; success signal concrete; CAP ids unique; lean. Pass 2 (Preservation): recon facts (single-quote style, no lint in CI, CONTRIBUTING absent) landed in Why/Constraints/Assumptions. OK.
- (decision) LICENSE added to the hygiene story (Jan request): repo carries a LICENSE file, README badge stops claiming UNLICENSED, package.json gains a license field. Default license = MIT (tasks.org anticipated an MIT badge); owner confirmation needed before committing the file.
- (capability) CAP-3 (license): the repository carries a LICENSE the README badge and package.json agree with, replacing the UNLICENSED placeholder.
- (decision) Story 1 review loop, human-approved renegotiations: quoteStyle single→double (tree measured uniformly double-quoted — 355/355 imports, ~250:4 assignments; single would flip ~6,200 strings across ~60 TS files, so the frozen no-churn guardrail wins); `lint:fix` uses `biome check --write` because Biome 2.x removed `--apply`. Recorded in the story's Spec Change Log with KEEP instructions. Review hardening: lint gate fails on warnings (`--error-on-warnings`), `files.includes` widened to `**` minus non-source exclusions (new source trees can't silently escape), assist organizeImports pinned explicitly, LICENSE/CONTRIBUTING pins added as scripts/repo-hygiene.test.ts, CI gained a lint red-path gate (seeded violation must fail and recover via fix).
