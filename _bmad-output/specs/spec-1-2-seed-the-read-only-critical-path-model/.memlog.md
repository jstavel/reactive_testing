---
topic: Story 1.2 — seed the read-only critical-path model (Kraken Pro order History, order book, portfolio) with QE-authored Gherkin input
updated: 2026-08-21T16:08
---

- (direction) QE (Jan) authors Gherkin scenarios with AI assistance as the human input driving what gets seeded — FR-2 'AI proposes, QE owns' applies to every model entry
- (decision) Committed .feature files are deliverables of this story, living in repo-root features/ — valid feature files per AD-9 file convention, not scratch notes
- (decision) Scope kept strictly to epic AC (epics.md Story 1.2); deferred-work items (runtime schema smoke test, FSM referential-integrity refinements) explicitly stay deferred
- (constraint) Gherkin is input/query layer only, never SSOT (FR-9/AD-9/AD-1); no Gherkin governance machinery built here — Epic 3 owns it
- (capability) CAP-1 seeded-read-only-model — intent: the committed model contains FSM states + dialog contracts for order History, order book, and portfolio so recording starts grounded; success: classification follows state-granularity.md (URL change → state, action → contract, data value → parameter), only read-only flows modeled (NFR-3), tsc --noEmit clean
- (capability) CAP-2 qe-authored-gherkin-input — intent: the QE prepares Gherkin scenarios (AI-assisted) as committed .feature files that name which states/contracts the seed must contain; success: features/ holds .feature scenarios whose steps each trace to a seeded state or contract, and no model entry lacks QE adjudication
- (constraint) No Playwright or test runtime installed — fsm.ts/contracts.ts stay dependency-free with abstract action/guard signatures (Epic 2 owns the runtime)
- (constraint) English-only identifiers and artifacts (NFR-4); never use the term 'aspect'
- (assumption) History/Main/Ledger is one state hosting filter-by-type/paginate/clear-filters contracts per the state-granularity.md example; exact state decomposition for order book and portfolio is settled during build via AI proposal + QE adjudication
- (question) Which concrete states make up the order-book and portfolio surfaces in the seed (epic AC pins History/Main/Ledger only)?
- (event) Self-validate pass 1 (coherence): PASS — both capabilities carry intent+success; constraints bend design; 4 non-goals; success signal testable
- (event) Self-validate pass 2 (preservation): PASS — epic AC, state-granularity rules, NFR-3/4, FR-2, AD-9, user's Gherkin direction, and strict-scope decision all landed in kernel or companions; wrapper ceremony (user-story phrasing) dropped on record
