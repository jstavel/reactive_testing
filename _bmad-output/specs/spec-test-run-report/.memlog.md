---
topic: Test Run Report
goal: Single self-contained HTML test report with Gherkin tree, progressive disclosure, screenshots, FSM state, and contract assertions
updated: 2026-09-10T22:41
---

- (direction) Source: brainstorm-intent.md from brainstorm-test-report session. Rich input, express mode - distill directly.
- (event) Self-validate Pass 1 (Coherence): All 5 capabilities have intent+success. Intents describe WHAT not HOW. Constraints bend design (single-file rules out dashboards, progressive disclosure rules out flat dumps, failure-only DOM rules out full snapshots). Non-goals explicit (6 items). Success signal concrete and testable. CAP IDs stable and unique. No decoration.
- (event) Self-validate Pass 2 (Preservation): All load-bearing claims from brainstorm-intent.md landed: single HTML (CAP-1), Gherkin tree (CAP-2), per-step evidence (CAP-3), embedded Gherkin (CAP-4), simple errors (CAP-5), MoSCoW split reflected in constraints+non-goals, bloat mitigations in constraints, FSM differentiator in success signal. Wrapper-only content dropped: Robot Framework comparison narrative, black/green/yellow hat commentary, process details.
- (event) Spec finalized. Assumptions and open questions carried forward for downstream resolution.
- (decision) Story breakdown: 5 stories agreed. S1 HTML scaffold+summary bar (spec_checkpoint), S2 Gherkin tree+progressive disclosure (spec_checkpoint), S3 per-step expandable evidence (no checkpoints), S4 FSM+contracts+DOM on failure (spec_checkpoint), S5 error messages (done_checkpoint).
- (event) stories.yaml written and validated against stories-schema: all 5 required fields present, ids unique and prefix-free (1-5), no status field, ids quoted strings with only digits. Each description two sentences pointing into SPEC capabilities.
- (note) 2026-09-10 status check: CAP-1/2/4 implemented as library code (reporter/html-report.ts, gherkin-snapshot.ts) and unit-tested; CAP-3 partial (per-step evidence type exists). The report has NO operator path — emitHtmlReport is only invoked from its own unit tests, no corpus run contains report.html. Tracked as epic-4 retro item-5 (FR-12/13 wiring) and epic-3 retro item-10 (library-only vs CLI decision).
- (decision) Report generation gets an operator CLI path (report:smoke): offline over a recorded corpus run — load plan + manifest, re-run validators through the offline runner (same engine as validate:smoke), derive per-scenario results, emit the self-contained report to {corpusDir}/{runId}/report.html. New CAP-6; closes the wiring half of epic-4 retro item-5.
- (constraint) The report CLI is offline-only: it reads recorded corpus evidence and never launches a browser, executes actions, or touches the live app (parity with validate:smoke, NFR-1 style purity).
- (assumption) Per-scenario results derive deterministically from offline validation results consumed in plan-step order per contractId; demo-verified against corpus efcb749d (18/18 checks → 14/14 scenarios, matching validate:smoke).
- (assumption) CLI argument surface mirrors validate:smoke: optional run id defaulting to the latest recorded run; report is written into the corpus run dir (emitHtmlReport's designed location, keeping evidence refs corpus-relative).
- (event) Self-validate update (2026-09-10). Pass 1 Coherence: CAP-6 has intent+success (WHAT-level, testable); offline constraint bends design (rules out live-run reports, browser launches); new assumptions are called-out inferences, not invented facts; existing CAP-1..5 untouched. Pass 2 Preservation: conversation claims landed (library-only status in Why, CLI engine parity, report location, result-derivation mapping, argument surface). Stories.yaml check: existing stories 1-5 still match the spec, but no story covers CAP-6 wiring — flagged for Story Breakdown.
- (decision) Story 6 agreed (2026-09-10, Jan confirmed): 'Operator CLI generates the report from a recorded run' — wire emitHtmlReport behind a report:smoke CLI (optional run id defaulting to latest recorded run; per-scenario results re-derived via the offline validator runner; report.html written into the run dir), implementing CAP-6. Checkpoints set by Jan: spec_checkpoint=false, done_checkpoint=true. invoke_dev_with: mirror validate:smoke's argument/error surface; pin the CLI at process level like bin/run-smoke.test.ts.
- (event) stories.yaml re-derived with story 6 (2026-09-10) and validated against stories-schema: ids '1'-'6' quoted strings, unique, prefix-free; required fields present; no status field; story 6 description two sentences pointing into SPEC CAP-6; checkpoints as confirmed by Jan (spec=false, done=true).
